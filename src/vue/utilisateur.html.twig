{% extends "base.html.twig" %}
{% block title %}
	{{parent()}}
	liste Utilisateur
{% endblock %}
{% block contenu %}

	<div class="table-responsive">
		<table class="table">
			<thead>
				<tr>
					<th scope="col">Email</th>
					<th scope="col">Nom</th>
					<th scope="col">Prenom</th>
					<th scope="col">Rôle</th>
					<th scope="col"></th>
				</tr>
			</thead>
			<tbody>
				{% for u in liste %}

					<tr>
						<td>{{u.email}}</td>
						<td>{{u.nom}}</td>
						<td>{{u.prenom}}</td>
						<td>{{u.libellerole}}</td>
						<td><a href="index.php?page=utilisateurModif&email={{u.email}}">Modifier</a></td>
					</tr>
				{% endfor %}
			</tbody>
		</table>
	</div>
{% endblock %}
